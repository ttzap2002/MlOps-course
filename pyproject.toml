[project]
name = "mlops-course"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pre-commit>=4.3.0",
    "pydantic-settings",
    "python-dotenv",
    "pyaml",
    "pytest",
    "pytest-dotenv>=0.5.2",
    "scikit-learn>=1.7.2",
]
